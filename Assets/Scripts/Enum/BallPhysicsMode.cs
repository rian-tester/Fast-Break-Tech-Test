public enum BallPhysicsMode
{
    // this enum at least accomodate
    // when ball is idle (physic on)
    // when ball is free (physic on)
    // when ball is bouncing (look at old logic)
    // when ball is fly to ring (look at old logic)  
}